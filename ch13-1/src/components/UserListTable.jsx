import { useState } from "react";

function UserListTable(user, setUsers) {
    const [ modalOpen, setModalOpen ] = useState(false);
    const [ editModal, setEditModal ] = useState(<></>);

    const handleEditOnClick = (e) => {
        
    };

    const handleDeleteOnClick = (e) => {

    };

}